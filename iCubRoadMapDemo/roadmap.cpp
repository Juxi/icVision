#include "roadmap.h"

Roadmap::Roadmap( /*yarp::os::Property prop*/ ) //: currentVertex(0), nextVertex(0)
{
	//yarp::os::Bottle& n = prop.findGroup("nodes");
	//for ( int i = 0; i < n.size(); i++ )
	//{
		//yarp::os::Bottle& q = n.get(i);
		//for ( int j = 0; j < q.size(); j++ )
		//{
			
		//}
	//}
}

Roadmap::~Roadmap() {}